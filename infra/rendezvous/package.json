{
  "name": "belay-rendezvous",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Belay cloud rendezvous - a stateless, cryptographically untrusted signaling introducer + TURN credential minter",
  "main": "dist/server.js",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
