{
  "name": "tether-host",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tether host agent - lets the Tether iOS app control this Windows or macOS machine",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build:native": "node -e \"const cp=process.getBuiltinModule('node:child_process');const p=process.platform;if(p!=='win32'&&p!=='darwin'){console.error('[build:native] no native helper for '+p);process.exit(1)}const win=p==='win32';const f=win?'powershell':'/bin/bash';const a=win?['-NoProfile','-ExecutionPolicy','Bypass','-File','native/build.ps1']:['native/build-mac.sh'];try{cp.execFileSync(f,a,{stdio:'inherit'})}catch(e){console.error('[build:native] '+(win?'native/build.ps1':'native/build-mac.sh')+' failed: '+e.message);process.exit(1)}\"",
    "build:native:win": "powershell -NoProfile -ExecutionPolicy Bypass -File native/build.ps1",
    "build:native:mac": "bash native/build-mac.sh",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts",
    "autostart": "node -e \"const cp=require('node:child_process');const p=process.platform;const a=process.argv.slice(1);if(p==='darwin'){cp.execFileSync('/bin/bash',['scripts/autostart-macos.sh',...a],{stdio:'inherit'})}else if(p==='win32'){cp.execFileSync('powershell',['-NoProfile','-ExecutionPolicy','Bypass','-File','scripts/autostart-windows.ps1','-Action',a[0]||'install'],{stdio:'inherit'})}else{console.error('[autostart] no recipe for '+p);process.exit(1)}\" --",
    "setup:whisper": "node scripts/setup-whisper.mjs",
    "sessions": "node scripts/list-sessions.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "optionalDependencies": {
    "node-pty": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
